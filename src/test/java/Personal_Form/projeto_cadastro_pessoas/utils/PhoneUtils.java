package Personal_Form.projeto_cadastro_pessoas.utils;

import Personal_Form.projeto_cadastro_pessoas.dto.request.PhoneDTO;
import Personal_Form.projeto_cadastro_pessoas.entities.Phone;
import Personal_Form.projeto_cadastro_pessoas.enums.PhoneType;

public class PhoneUtils {
    /*
    private static final String PHONE_NUMBER = "1199999-9999";
    private static final PhoneType PHONE_TYPE = PhoneType.MOBILE;
    private static final long PHONE_ID = 1L;

    public static PhoneDTO createFakeDTO() {
        return PhoneDTO.builder()
                .number(PHONE_NUMBER)
                .type(PHONE_TYPE)
                .build();
    }

    public static Phone createFakeEntity() {
        return Phone.builder()
                .id(PHONE_ID)
                .number(PHONE_NUMBER)
                .type(PHONE_TYPE)
                .build();
    }

     */
}
